#Trả lời câu hỏi số 4 chương 5
"""
1. Nhóm chuyển đổi kiểu chữ:

s.lower(): Chuyển toàn bộ chuỗi sang chữ thường.

s.upper(): Chuyển toàn bộ chuỗi sang chữ IN HOA.

s.title(): Viết hoa chữ cái đầu của mỗi từ trong chuỗi.

s.capitalize(): Chỉ viết hoa chữ cái đầu tiên của cả chuỗi.

2. Nhóm làm sạch chuỗi (Xử lý khoảng trắng):

s.strip(): Loại bỏ khoảng trắng ở cả hai đầu chuỗi (thường dùng để làm sạch dữ liệu nhập).

s.lstrip(): Chỉ loại bỏ khoảng trắng bên trái.

s.rstrip(): Chỉ loại bỏ khoảng trắng bên phải.

3. Nhóm Tách và Ghép chuỗi (Quan trọng):

s.split(kí_tự): Tách chuỗi thành một Danh sách (List) dựa trên kí tự phân cách.

Ví dụ: "a,b,c".split(",") → ['a', 'b', 'c']

"kí_tự".join(list): Ghép các phần tử trong List thành một chuỗi.

Ví dụ: "-".join(['a', 'b', 'c']) → "a-b-c"

4. Nhóm Tìm kiếm và Thay thế:

s.replace(cũ, mới): Thay thế chuỗi con cũ bằng chuỗi con mới.

s.find(chuỗi_con): Tìm vị trí index đầu tiên xuất hiện của chuỗi con (trả về -1 nếu không tìm thấy).

s.count(chuỗi_con): Đếm số lần xuất hiện của chuỗi con trong chuỗi mẹ.

5. Nhóm Kiểm tra dữ liệu (Trả về True/False):

s.startswith(chuỗi_con): Kiểm tra chuỗi có bắt đầu bằng chuỗi con không.

s.endswith(chuỗi_con): Kiểm tra chuỗi có kết thúc bằng chuỗi con không.

s.isdigit(): Kiểm tra chuỗi có phải toàn là chữ số không.

s.isalpha(): Kiểm tra chuỗi có phải toàn là chữ cái không.
"""